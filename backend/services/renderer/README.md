# Renderer Service

Placeholder for video rendering. The API creates render tasks and video asset records; the renderer
will consume those tasks and update `VideoAsset` status, URLs, duration, and errors.
